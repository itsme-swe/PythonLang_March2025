# Default Argument


def add(a, b=0, c=0):

    return a + b + c


sum = add(5, 5)

print(sum)

"""
🔸 default argument should always be on right side and keyword should be on left side.
And, default argument can be created only once.
"""
